#include"w_day.h"

day::day(){
 
}


/*void day::setDayWeather(const weather& we){
    w.setCondition(we.getCondition());
    w.setDate(we.getDate());
    w.setMaxTemperature(we.getMaxTemperature());
    w.setMinTemperature(we.getMinTemperature());
    w.setSunnyHours(we.getSunnyHours());
}*/

day::~day(){

}